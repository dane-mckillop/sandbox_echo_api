Purpose:
Simple api which will echo any parameter(s) passed to it in the response object.


Step-by-Step SetupRun the Application:bash

dotnet run

The API will start, typically at https://localhost:5001 or http://localhost:5000 (check the terminal output).